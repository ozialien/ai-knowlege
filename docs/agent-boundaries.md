# Agent Boundaries

retrieval agent:
- owns document search and grounding

code agent:
- owns repo-aware analysis and change suggestions

ops agent:
- owns runbook lookup and human approval requirement

Next step:
- replace local dispatch with a protocol transport and agent cards/metadata
