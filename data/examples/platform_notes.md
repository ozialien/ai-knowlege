# Platform Notes

Qdrant is used as the vector database for storing embeddings and retrieving nearby chunks.

FastAPI is used as the API layer.

LangGraph is used as a controlled workflow scaffold for agent patterns.

Ollama serves the local chat model and embedding model.

MLflow stores simple run metadata for chat, ingestion, and RAG execution.
