FROM python:3.11-slim
WORKDIR /app
COPY coordinator /app/coordinator
COPY agents /app/agents
ENV PYTHONPATH=/app
RUN pip install fastapi uvicorn pydantic
CMD ["uvicorn", "coordinator.main:app", "--host", "0.0.0.0", "--port", "8010"]
