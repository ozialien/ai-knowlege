# Platform Notes

Qdrant is used as the vector database for storing embeddings and retrieving similar chunks.

Open WebUI is a self-hosted local interface for chatting with models.

LangGraph is used here to show agent workflows, checkpoints, and thread state.

Ollama serves the local chat model and embedding model.

MLflow is used for basic event logging in this training lab.
